name: Build Flutter APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'

      - name: Setup project
        run: |
          cp -r lib lib_backup
          cp -r assets assets_backup
          cp pubspec.yaml pubspec_backup.yaml
          rm -rf android ios web linux macos windows test
          flutter create . --org id.my.zesbe --project-name lumina_ai --platforms android
          rm -rf lib assets
          mv lib_backup lib
          mv assets_backup assets
          mv pubspec_backup.yaml pubspec.yaml

      - name: Configure Android for audio_service
        run: |
          # 1. AndroidManifest.xml - CRITICAL: Use AudioServiceActivity!
          cat > android/app/src/main/AndroidManifest.xml << 'EOF'
          <manifest xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools">
              
              <uses-permission android:name="android.permission.INTERNET"/>
              <uses-permission android:name="android.permission.WAKE_LOCK"/>
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
              <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
              <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
              
              <application
                  android:label="Lumina AI"
                  android:name="${applicationName}"
                  android:icon="@mipmap/ic_launcher"
                  android:usesCleartextTraffic="true"
                  tools:targetApi="34">
                  
                  <!-- CRITICAL: Activity MUST use AudioServiceActivity for notifications! -->
                  <activity
                      android:name="com.ryanheise.audioservice.AudioServiceActivity"
                      android:exported="true"
                      android:launchMode="singleTop"
                      android:taskAffinity=""
                      android:theme="@style/LaunchTheme"
                      android:hardwareAccelerated="true"
                      android:windowSoftInputMode="adjustResize"
                      android:showWhenLocked="true"
                      android:turnScreenOn="true">
                      <meta-data
                          android:name="io.flutter.embedding.android.NormalTheme"
                          android:resource="@style/NormalTheme"/>
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  
                  <!-- Audio Service -->
                  <service 
                      android:name="com.ryanheise.audioservice.AudioService"
                      android:foregroundServiceType="mediaPlayback"
                      android:exported="true"
                      tools:ignore="Instantiatable">
                      <intent-filter>
                          <action android:name="android.media.browse.MediaBrowserService"/>
                      </intent-filter>
                  </service>
                  
                  <!-- Media Button Receiver -->
                  <receiver 
                      android:name="com.ryanheise.audioservice.MediaButtonReceiver"
                      android:exported="true"
                      tools:ignore="Instantiatable">
                      <intent-filter>
                          <action android:name="android.intent.action.MEDIA_BUTTON"/>
                      </intent-filter>
                  </receiver>
                  
                  <meta-data android:name="flutterEmbedding" android:value="2"/>
              </application>
          </manifest>
          EOF
          
          # 2. build.gradle
          sed -i 's/minSdk.*/minSdk = 21/' android/app/build.gradle
          sed -i 's/targetSdk.*/targetSdk = 34/' android/app/build.gradle
          sed -i 's/compileSdk.*/compileSdk = 34/' android/app/build.gradle
          
          # 3. Keep resources file (prevent icon stripping)
          mkdir -p android/app/src/main/res/raw
          cat > android/app/src/main/res/raw/keep.xml << 'KEEPEOF'
          <?xml version="1.0" encoding="utf-8"?>
          <resources xmlns:tools="http://schemas.android.com/tools"
            tools:keep="@drawable/*,@mipmap/*" />
          KEEPEOF

      - name: Copy custom app icon
        run: |
          cp assets/icons/mipmap-mdpi/ic_launcher.png android/app/src/main/res/mipmap-mdpi/
          cp assets/icons/mipmap-mdpi/ic_launcher_round.png android/app/src/main/res/mipmap-mdpi/
          cp assets/icons/mipmap-hdpi/ic_launcher.png android/app/src/main/res/mipmap-hdpi/
          cp assets/icons/mipmap-hdpi/ic_launcher_round.png android/app/src/main/res/mipmap-hdpi/
          cp assets/icons/mipmap-xhdpi/ic_launcher.png android/app/src/main/res/mipmap-xhdpi/
          cp assets/icons/mipmap-xhdpi/ic_launcher_round.png android/app/src/main/res/mipmap-xhdpi/
          cp assets/icons/mipmap-xxhdpi/ic_launcher.png android/app/src/main/res/mipmap-xxhdpi/
          cp assets/icons/mipmap-xxhdpi/ic_launcher_round.png android/app/src/main/res/mipmap-xxhdpi/
          cp assets/icons/mipmap-xxxhdpi/ic_launcher.png android/app/src/main/res/mipmap-xxxhdpi/
          cp assets/icons/mipmap-xxxhdpi/ic_launcher_round.png android/app/src/main/res/mipmap-xxxhdpi/

      - run: flutter pub get
      - run: flutter build apk --release

      - uses: actions/upload-artifact@v4
        with:
          name: lumina-ai-v2.0.0
          path: build/app/outputs/flutter-apk/app-release.apk
